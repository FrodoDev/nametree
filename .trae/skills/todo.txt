🌟 亮点：
* 选中某个节点，它会呼吸
* 现在选中全部hu节点，一起呼吸的感觉，有阿凡达上那棵圣树的感觉
* 加一个边框和填充颜色一起设置的涂色框
* 添加一些经典配色方案，选定一个方案后，出现这个色系的条形色卡


tab 键默认延生树枝 ok
持导出 png ok
调整树干和树根，让它显得更自然 ok
调整树枝角度，以及树枝长度，让树枝更像树冠 ok

树枝拖动，只是改父节点，节点自己的子节点跟随 ok
树干也是可以当成父节点的 ok
树枝的上下位置，可以由拖动位置决定 ok
树枝的拖动逻辑，完全应用到树根，但是树根因为没有上下关系，所以要用虚拟位置表示 ok
根节点和树根默认用黑色 ok
选中节点后，它跟父节点连接的树根或树枝也要呼吸，方便看清楚连接路线 ok
跟 xmind 一样，可以用cmd+单选选中多个节点，也可以拖动鼠标选中 ok
    可以批量拖动，从新赋予父节点
    可以批量修改边框颜色、填充颜色等
    多选逻辑这样做：
        1. 不限制选中的是什么节点，节点之间有什么关系
        2. 可以批量修改选中节点的属性，比如边框颜色、填充颜色等，以及后面会支持的字体修改等
填充颜色：
    边框和填充颜色，在他们后边，保留 9 个历史选中的颜色，这样能让设置颜色效率提高 ok
    历史颜色卡，上下更长的长方形叠放一起。选中的时候有 hover 效果 ok
    加一个边框和填充颜色一起设置的涂色框 ok
    添加一些经典配色方案，选定一个方案后，出现这个色系的条形色卡。方案用 ipod 老样式，选定色系后的色卡也是。
备注：
    没有备注的节点，选中节点时，备注栏默认折叠，反之展开 ok
    原本有备注的节点，删除备注后，再切换到节点，备注栏恢复默认折叠 ok
    有备注的节点，有备注的标识 ok
    有备注的节点，鼠标悬浮在上面的时候，浮层现实备注内容 ok
背景：
    给背景添加一个指定的照片  试验后发现不太可行，先搁置




节点连接
支持多棵树，目前只有一颗，树之间如何关联
多棵树，组成一片树林，比如大学的学科，可以把一个学期的课程表达出来

slogan：把想法长成一棵树

做几个非常漂亮的内置模板 模板比空白画布更适合商业产品。比如：
- 人物研究树
- 读书笔记树
- 论文/文章写作树
- 产品方案树
- 关系故事树
- 学科知识树
- 项目复盘树


- Markdown 导出
- 图片/PDF 导出
- JSON/.nt 文件稳定保存
- 从缩进文本生成树
- 从树生成大纲文本

字体支持

缩放，只显示第一层级的节点